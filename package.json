{
   "devDependencies": {
      "webpack": "^5.75.0",
      "webpack-cli": "^4.10.0",
      "webpack-dev-server": "^4.10.0"  // If you want to serve your app locally during development
   },
   "dependencies": {
      "three": "^0.149.0"
   },
   "scripts": {
      "build": "webpack --mode production",  // Build your app for production
      "start": "webpack serve --open --mode development"  // Serve your app locally during development
   }
}
